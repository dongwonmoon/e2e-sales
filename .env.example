# Docker-compose에서 사용할 환경 변수를 정의합니다.
# Linux/macOS 사용자의 경우, AIRFLOW_UID를 `id -u` 명령의 결과값으로 설정하면
# Airflow 컨테이너가 생성하는 파일의 권한 문제를 방지할 수 있습니다.
AIRFLOW_UID=50000
AIRFLOW_GID=0

# 프로젝트 데이터베이스 (PostgreSQL) 설정
PROJECT_DB_HOST=project-db
PROJECT_DB_PORT=5433
PROJECT_DB_USER=sales
PROJECT_DB_PASSWORD=sales
PROJECT_DB_NAME=sales

# MLflow 설정
# 컨테이너 간 통신을 위해 서비스 이름을 호스트로 사용합니다.
MLFLOW_TRACKING_URI=http://mlflow-server:5000
MLFLOW_EXPERIMENT_NAME=sales_daily

# 모델 서빙 API (FastAPI) 설정
PREDICTION_API_URL=http://prediction-service:8000
